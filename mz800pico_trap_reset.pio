.program mz800pico_trap_reset

.wrap_target

wait_iorq_low:
    wait 1 gpio 19
    irq set 3
.wrap

% c-sdk {
#include "hardware/pio.h"
#include "mz800pico_trap_reset.pio.h"

void mz800pico_trap_reset_init(PIO pio, uint sm) {
    uint offset = pio_add_program(pio, &mz800pico_trap_reset_program);

    pio_sm_config c = mz800pico_trap_reset_program_get_default_config(offset);

    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}
%}
